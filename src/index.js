import React from "react"
import ReactDOM from "react-dom"
import TodoContainer from "./new/components/TodoContainer"

import "./new/App.css"

ReactDOM.render(<TodoContainer />, document.getElementById("root"))